import styled from "styled-components"

export const StyledNotContacts = styled.div`
    background-color: rgba(0,0,0, .1);
    width: 100%;
    height: 120px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 20px;
    text-align: center;
    border-radius: 8px;

    p {
        color: black;
        font-size: 15px;
        font-weight: 600;
    }

    span {
        color: black;
        font-size: 15px;
        font-weight: 600;
    }

`